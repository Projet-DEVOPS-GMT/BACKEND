spring:
  datasource:
    url: jdbc:mysql://mysql:3306/testdb?serverTimezone=UTC&useSSL=false
    username: testuser
    password: testpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
